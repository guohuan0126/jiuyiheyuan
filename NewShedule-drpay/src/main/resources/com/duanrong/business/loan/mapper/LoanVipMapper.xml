<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duanrong.business.loan.mapper.LoanVipMapper">
	<resultMap type="loanVip" id="loanVipRM">
		<id property="id" column="id" />
		<result property="loanId" column="loan_id" />
		<result property="userId" column="user_id" />
		<result property="status" column="status" />
		<result property="time" column="time" />
		<result property="realname" column="realname" />
		<result property="mobileNumber" column="mobile_number" />
	</resultMap>
	 <select id="pageInfo"  resultMap="loanVipRM">
			SELECT
				r.*,o.realname as realname,o.mobile_number as mobile_number
			FROM	
				loan_vip r
			LEFT JOIN user o ON r.user_id = o.id
			where r.status=1
			  ORDER BY r.time
	 </select>
	<insert id="insert" parameterType="loanVip">
		insert into loan_vip(loan_id, user_id, status,time) values(#{loanId}, #{userId}, #{status}, #{time})
	</insert>
	
	<update id="update" parameterType="loanVip">
		UPDATE loan_vip set status=0
		WHERE id = #{id}
	</update>
	<select id="get" parameterType="string" resultMap="loanVipRM">
		SELECT *
		FROM loan_vip
		WHERE user_id = #{userId} and status=1
	</select>
</mapper>