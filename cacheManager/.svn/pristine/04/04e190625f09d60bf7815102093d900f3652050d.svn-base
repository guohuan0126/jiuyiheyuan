package com.duanrong.business.cache.service;

import com.duanrong.business.cache.model.CacheKey;

import base.pagehelper.PageInfo;

public interface CacheService {

	PageInfo<CacheKey> pageInfo(int pageNo, int pageSize,
			CacheKey cacheKey);
	
	CacheKey get(String id);
	
	void insert(CacheKey cacheKey);
	
	
	void update(CacheKey cacheKey);
	
	void delete(String id);
}
