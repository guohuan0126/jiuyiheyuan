<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

	<!-- 扫描 controller -->
	<context:component-scan base-package="com.duanrong.cps.controller" />

	<bean
		class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter">
		<property name="messageConverters">
			<list>
				<bean class="base.util.MappingFastjsonHttpMessageConverter">
					<property name="supportedMediaTypes" value="application/json;charset=UTF-8" />
				</bean>
			</list>
		</property>
	</bean>

	<!-- 注册驱动 -->
	<!-- 注解驱动尽量放后面， 如果放在自定义的数据格式转化器之前， spring 会调用默认的格式转换器，导致自定义的格式转换器不起作用 -->
	<mvc:annotation-driven />

	<!-- 对模型视图名称的解析，即在模型视图名称添加前后缀,在requestmapping输入的地址后自动调用该类进行视图解析 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="viewClass"
			value="org.springframework.web.servlet.view.JstlView" />
		<property name="prefix" value="/WEB-INF/jsp/" /><!-- 跳转页面的前缀路径 -->			
		<property name="suffix" value=".jsp"></property><!-- 跳转页面后缀 -->
	</bean>
	
	<!-- spring MVC 文件上传解析器  -->
	<bean class="org.springframework.web.multipart.commons.CommonsMultipartResolver">  
        <property name="defaultEncoding" value="utf-8"></property>   
        <property name="maxUploadSize" value="10485760000"></property>  
        <property name="maxInMemorySize" value="40960"></property>  
   </bean> 

	<!-- 设置使用注解的类所在的jar包 -->
	<!-- <context:component-scan base-package="" /> -->
	<!-- <mvc:interceptors> 
		<mvc:interceptor> 
			<mvc:mapping path="/rest/**" /> <bean class="" />
		 </mvc:interceptor> 
	 </mvc:interceptors> -->
<!-- 自定义拦截器 -->
	<!-- 自定义拦截器 用户是否登陆 是否在ip的白名单内 使用超级密码  -->
   <!-- <mvc:interceptors>
		<mvc:interceptor>
		根据超级密码和ip白名单进行拦截
			<mvc:mapping path="/*"/>
			<bean class="com.duanrong.cps.interceptor.PermissionIntercepter"/>
		</mvc:interceptor>
		<mvc:interceptor>
		 拦截未登陆用户 
			 <mvc:mapping path="/*/**"/>
			<bean class="com.duanrong.cps.interceptor.loginInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>  -->
	
		<!-- 拦截器 -->
	<mvc:interceptors>
		<!-- 多个拦截器,顺序执行 -->
		<!-- 权限拦截器 -->
		
		 <!-- <mvc:interceptor>
			<mvc:mapping path="/**"/>
			<bean class="com.duanrong.authentication.interceptor.AuthenticationInterceptor"></bean>
		</mvc:interceptor>   -->
		
		<mvc:interceptor>
	    	<mvc:mapping path="/BJSInter/*"/>
	    	<bean class="com.duanrong.cps.controllerHelper.BjsInterceptor"></bean>
	    </mvc:interceptor>
		
	</mvc:interceptors>
	
	
</beans>