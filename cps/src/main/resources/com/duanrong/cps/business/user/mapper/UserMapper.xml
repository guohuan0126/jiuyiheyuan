<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duanrong.cps.business.user.mapper.UserMapper">
	<resultMap type="user" id="userRM">
		<id property="userId" column="id" />
		<result property="username" column="username" />
		<result property="email" column="email" />
		<result property="registerTime" column="register_time" />
		<result property="mobileNumber" column="mobile_number" />
		<result property="status" column="status" />
		<result property="password" column="password" />
		<result property="cashPassword" column="cash_password" />
		<result property="postAddress" column="post_address" />
		<result property="postCode" column="post_code" />
		<result property="realname" column="realname" />
		<result property="idCard" column="id_card" />		
	</resultMap>
	
	<resultMap type="role" id="roleRM">
		<id property="id" column="id"/>
		<result property="name" column="name" />
		<result property="description" column="description" />
		<result property="status" column="status" />			
	</resultMap>
	
	<!-- 根据手机号查询用户  -->
	<select id="getUserByMobileNumber" resultMap="userRM" parameterType="string">		
		select * from user where mobile_number = #{mobileNumber}
	</select>
	<select id="getUserById" resultMap="userRM" parameterType="string">		
		select * from user where id = #{id}
	</select>
	<select id="getUserByEmail" resultMap="userRM" parameterType="string">		
		select * from user where email = #{email}
	</select>
	<select id="getRoleByUserId" resultType="List" parameterType="string">
	select  * from user_role where user_id= #{userId}
	</select>	
	<select id="getCountUserAndRole" resultType="Integer" parameterType="map">
	select  count(*) from user_role where user_id= #{userId} and role_id =#{roleId}
	</select>
	
	<!-- 查询和投之家相联的老用户没有实名信息的用户 -->
	<select id="getTouzhijiaUser" resultMap="userRM" parameterType="map">
		SELECT platform_user_relation.*,user.`id_card`,user.`realname` FROM platform_user_relation,USER 
		 WHERE user.`id`=platform_user_relation.`userID` AND 
                 platform_user_relation.type='TouZhiJia' AND platform_user_relation.whether_new='1' AND user.`id_card` IS NULL
	</select>
		
	 
	<select id="getUserInfo" resultMap="userRM"
		parameterType="map">
		SELECT u.* FROM
		`user` u,user_other_info uoi
		<where>
		    u.id = uoi.id
		    AND uoi.user_source like '%xicaiwang%'
			<if test="startdate != null and startdate != ''">
				AND u.register_time &gt;=#{startdate}
			</if>
			<if test="enddate != null and enddate != ''">
				AND u.register_time &lt;=#{enddate}
			</if>
		</where>
		order by u.register_time desc
		limit #{start},#{pagesize}
	</select>
	
	<select id="getTotalUser" resultType="int"
		parameterType="map">
		SELECT count(*) FROM
		`user` u,user_other_info uoi
		<where>
		    u.id = uoi.id
		    AND uoi.user_source like '%xicaiwang%'
			<if test="startdate != null and startdate != ''">
				AND u.register_time &gt;=#{startdate}
			</if>
			<if test="enddate != null and enddate != ''">
				AND u.register_time &lt;=#{enddate}
			</if>
		</where>
	</select>
	
	
	<select id="getUserSource" resultType="string" parameterType="string">
	SELECT user_source FROM user_other_info WHERE id = #{userId}
	</select>
</mapper>